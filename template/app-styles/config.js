// Configuration File for UI Design Styles Showcase
// You can modify these values to customize the display and behavior

export const config = {
    // Animation durations (in milliseconds)
    animations: {
        fadeTime: 500,
        transitionSpeed: 300
    },
    
    // Interactive settings
    interactive: {
        enableSounds: true,
        enableAnimations: true
    },
    
    // Personalization
    customization: {
        // Set to true to enable dark mode for the entire app (not just the demo)
        darkModeDefault: false,
        
        // Set your preferred accent color
        accentColor: '#4285F4'
    }
};

